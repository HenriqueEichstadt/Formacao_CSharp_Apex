namespace Capitulo11.ExercicioExtra3
{
    internal class Program
    {
        /*
         Crie um projeto ConsoleApplication e implemente os seguintes requisitos. Seu projeto deverá:
        
            a. Criar uma interface chamada 'IVeiculo', que contenha os métodos 'Acelerar' e 'Desacelerar'.
            b. Criar duas classes que implementam a interface 'IVeiculo': 'Carro' e 'Moto'.
            c. Cada uma das classes deve implementar os métodos 'Acelerar' e 'Desacelerar' de acordo com as suas próprias regras (por exemplo, a moto pode acelerar mais rapidamente que o carro).
            d. Cada veículo deve ter um modelo e uma velocidade atual.
            e. O método principal deve cadastrar cada um dos veículos criados e atribuí-los a uma lista de 'IVeiculo'.
            f. O método principal deve permitir que o usuário escolha qual veículo ele deseja utilizar (por meio de um menu de opções) e então permitir que ele acelere ou desacelere o veículo selecionado.
            g. O método principal deve imprimir na tela as propriedades atualizadas do veículo selecionado após cada transação realizada.
         */
        static void Main(string[] args)
        {
            Console.WriteLine("Resolva...");
        }
    }
}